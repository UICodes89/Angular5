import { HighiliterDirective } from './highiliter.directive';

describe('HighiliterDirective', () => {
  it('should create an instance', () => {
    const directive = new HighiliterDirective();
    expect(directive).toBeTruthy();
  });
});
